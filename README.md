# LibraryDesk Pro Backend

## Run locally
1. Install Python 3.10+
2. Open terminal in this folder
3. `pip install -r requirements.txt`
4. `python app.py`
5. Open `http://127.0.0.1:5000`

## Admin
- Username: `admin`
- Password: `admin123`

## Demo users
- `library_demo1` / `demo123`
- `library_demo2` / `demo123`
- `pg_demo1` / `demo123`
- `pg_demo2` / `demo123`

Each demo account starts with 30 days access. Admin can suspend, activate, extend +30 days, or create new demo accounts.

For production deployment, change the secret key and use a production database/server.
